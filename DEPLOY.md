# 🚀 วิธี Deploy Dashboard ลง GitHub Pages

## ขั้นตอนที่ 1: เตรียมข้อมูล

แปลง Excel เป็น JSON (สำหรับ GitHub Pages):

```bash
python convert_excel_to_json.py
```

ไฟล์ `data.json` จะถูกสร้างขึ้นอัตโนมัติ

## ขั้นตอนที่ 2: สร้าง Repository บน GitHub

1. ไปที่ https://github.com
2. คลิก "New repository"
3. ตั้งชื่อ เช่น `elderly-dashboard-2566`
4. เลือก Public
5. คลิก "Create repository"

## ขั้นตอนที่ 3: Upload โค้ดขึ้น GitHub

เปิด Terminal/PowerShell แล้วรันคำสั่ง:

```bash
# ไปที่โฟลเดอร์โครงการ
cd "c:\Users\User\OneDrive\เดสก์ท็อป\A1"

# Initialize Git repository
git init

# Add all files
git add .

# Commit
git commit -m "Initial commit: Dashboard ข้อมูลผู้สูงอายุ 2566"

# เชื่อมต่อกับ GitHub (แทนที่ YOUR_USERNAME ด้วยชื่อผู้ใช้ของคุณ)
git remote add origin https://github.com/YOUR_USERNAME/elderly-dashboard-2566.git

# Push to GitHub
git branch -M main
git push -u origin main
```

## ขั้นตอนที่ 4: เปิดใช้งาน GitHub Pages

1. ไปที่ Repository บน GitHub
2. คลิก **Settings** (แท็บด้านบน)
3. เลื่อนลงไปที่ **Pages** (เมนูด้านซ้าย)
4. ในส่วน **Source**:
   - Branch: เลือก `main`
   - Folder: เลือก `/ (root)`
5. คลิก **Save**

## ขั้นตอนที่ 5: รอและเปิดใช้งาน

รอประมาณ 1-2 นาที จากนั้น:

1. GitHub จะแสดง URL ของ Dashboard เช่น:
   ```
   https://YOUR_USERNAME.github.io/elderly-dashboard-2566/
   ```

2. คลิกลิงก์เพื่อเปิด Dashboard

## 🎉 เสร็จสิ้น!

Dashboard ของคุณพร้อมใช้งานแล้ว! คุณสามารถแชร์ลิงก์ให้คนอื่นได้เลย

---

## 🔄 วิธีอัพเดทข้อมูลใหม่

เมื่อมีข้อมูลใหม่:

```bash
# 1. แปลง Excel เป็น JSON
python convert_excel_to_json.py

# 2. Add, Commit และ Push
git add data.json
git commit -m "Update data"
git push

# 3. รอ 1-2 นาที Dashboard จะอัพเดทอัตโนมัติ
```

---

## ⚠️ หมายเหตุสำคัญ

### ไฟล์ที่ต้อง Push ขึ้น GitHub:
- ✅ `index.html`
- ✅ `styles.css`
- ✅ `app.js`
- ✅ `data.json` (สำคัญมาก!)
- ✅ `README.md`
- ✅ `.gitignore`

### ไฟล์ที่ไม่จำเป็น (Optional):
- `รวมข้อมูลผู้สูงอายุ_2566.xlsx` - ไม่จำเป็นสำหรับ GitHub Pages
- `convert_excel_to_json.py` - เก็บไว้สำหรับอัพเดทข้อมูล

---

## 🐛 แก้ปัญหา

### ถ้า Dashboard ไม่แสดงข้อมูล:
1. ตรวจสอบว่ามีไฟล์ `data.json` ใน Repository
2. ตรวจสอบ Console ใน Browser (F12)
3. รอ 2-3 นาที แล้วลองใหม่

### ถ้า GitHub Pages ไม่ทำงาน:
1. ตรวจสอบว่า Repository เป็น Public
2. ตรวจสอบว่าเปิดใช้งาน Pages แล้ว
3. ตรวจสอบว่าเลือก branch main

---

## 📱 ตัวอย่าง URL

ถ้าชื่อผู้ใช้ของคุณคือ `john` และชื่อ repo คือ `elderly-dashboard-2566`:

```
https://john.github.io/elderly-dashboard-2566/
```

คุณสามารถแชร์ URL นี้ให้ใครก็ได้!
